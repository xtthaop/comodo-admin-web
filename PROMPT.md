| 页面     | 模块     | 功能项     | 描述                             | 进度   | 备注           |
|----------|---------|-----------|----------------------------------|-------|---------------|
| **登录页** | **登录表单** | **输入用户名密码** | <ol><li>页面加载成功后用户名输入框为聚焦状态；<li>用户名不能为空且最长输入 64 个字符；<li>用户名输入框右侧显示清除按钮，点击可以清除用户名输入框中的内容；<li>密码不能为空且最长输入 20 个字符；<li>在密码输入框中输入大写字母时提示已开启大写；<li>密码输入框右侧显示清除和显示密码按钮，点击可以完成对应功能；<li>用户名密码输入完成后点击登录按钮触发拼图验证； | ***100%*** |  |
| **登录页** | **登录表单** | **拼图验证码** | <ol><li>用户名密码输入完成后点击登录按钮弹出拼图验证码；<li>拼图验证码包括随机请求的一张图像和从图像中抠出的一块拼图；<li>抠图时选择的位置在图像的右半部分，抠出后原图像位置显示阴影；<li>被抠出的拼图放在图像最左边，保证一定的移动空间；<li>请求图像和抠出拼图的过程中显示加载中状态；<li>请求失败时显示加载失败；<li>加载成功后在拼图验证码下方显示滑轨和滑块；<li>点击左上角换一张按钮可更换拼图验证码；<li>点击右上角关闭按钮可关闭拼图验证码；<li>鼠标拖动滑块可移动拼图，松开鼠标结束拖动；<li>结束拖动后将滑块移动的距离与实际拼图位置做对比确定是否验证成功；<li>验证失败时提示拼图验证失败并重新请求拼图验证码；<li>验证成功后验证用户名密码；<li>用户名错误或者密码错误则显示用户名与密码不匹配并关闭拼图验证码弹窗，旧已验证正确的验证码要保证失效；<li>验证拼图或者用户名密码的过程中不可操作滑块、更换验证码，关闭拼图验证弹窗再打开要保持验证中的验证码状态；<li>验证码及用户名密码验证成功后滑轨显示拼接成功即登录成功； | ***100%*** |  |
| **登录页** | **其他** | **其他** | <ol><li>未登录状态访问不需登录便可访问的页面时可直接进入，访问需要登录才能访问的页面时会直接跳转到登录页面，并设置登录页地址的查询字符串为将要访问页面的查询字符串，并在查询字符串中添加 \_\_redirect\_\_ 参数设置其值为将要访问的页面地址，后续简称携带 redirect 参数；<li>登录成功后如果查询字符串中存在携带 redirect 参数则跳转到指定的页面，这意味着 \_\_redirect\_\_ 参数名为保留名，应避免重名，一旦发生重名会出现丢失 \_\_redirect\_\_ 参数的情况；<li>登录成功后默认跳转到首页，此时在浏览器地址栏输入登录页地址进行访问会立即重定向到首页；<li>登录状态最长可以保持 7 天，在 7 天内没有任何操作则会强制重新登录，在临近到期的最后 30 分钟内发起操作会重新延长登录状态到 7 天；<li>每次发起接口请求都会检查用户的登录状态，如果失效会提示用户登录状态已失效，用户点击确认或者刷新页面后会清除用户信息回到登录页面并强制刷新页面，此时会携带 redirect 参数，保证再次登录成功后可直接回到原页面位置；<li>每次发起接口请求同样会检查用户账号是否被禁用，如果被禁用会提示用户账号已被禁用，用户点击确认或者刷新页面会清除用户信息回到登录页面并强制刷新页面，不携带 redirect 参数；<li>每次发起接口请求还会检查用户是否拥有此接口权限，如果未给用户分配此接口权限，则提示请求被拒绝，如果已分配则记录操作日志；<li>首次登录成功后会请求用户信息和用户拥有的菜单，用户信息包括用户名、用户角色、用户拥有的按钮权限标识，拥有角色标识为 admin 角色的用户拥有全部的按钮权限及菜单；<li>用户信息请求成功则继续请求用户菜单，拥有角色标识为 admin 角色的用户拥有所有菜单，其他角色用户根据权限获取菜单，菜单顺序应按设置的排序返回。与此同时，获取全部的菜单路径，目的是为了用户在访问未拥有的页面时提示 401 无权限；<li>每次刷新页面会重新请求用户信息和用户拥有的菜单；<li>将菜单转换成渲染菜单及注册路由需要的格式，将全部的菜单路径转换为路径正则；<li>用户信息与用户菜单路由请求过程中，页面顶部显示进度条；<li>退出登录时应清除所有用户信息并强制刷新页面，保证用户令牌彻底失效，携带 redirect 参数跳转到登录页；<li>记录用户的登录日志； | ***90%*** | <ol><li>验证登录日志是否记录正确；<li>验证操作日志是否记录正确； |
| **布局** | **侧边栏** | **侧边栏** | <ol><li>左侧侧边栏支持通过系统设置更换配色方案；<li>系统标识可正常显示，系统标识包括系统 logo 和系统名称。更换系统 logo 直接替换 public/logo.png 即可，登录页和浏览器标签栏的 logo 同时被替换。更换系统名称修改 src/setting.js 中的 systemName 即可，登录页和浏览器标签栏的系统名称同时被替换。通过修改 src/setting.js 中的 sidebarLogo 可控制系统 logo 和系统名称初始时默认显示还是隐藏，通过修改系统设置中的是否显示系统标识也可控制并会被浏览器记住，浏览器记住的选项值优先；<li>菜单可正常显示用户所有的菜单，支持多级嵌套；<li>点击折叠或展开按钮图标可控制侧边栏的折叠展开，折叠时系统标识只显示系统 logo，菜单只显示菜单图标，鼠标移动到菜单上时显示菜单名称或者弹出下一级菜单；<li>系统检测到当前可视区域宽度小于 992px 时，会认为在移动端小屏幕上浏览，此时会隐藏侧边栏的显示拓展出更大的浏览空间，控制侧边栏展开折叠的按钮开始控制侧边栏的隐藏和显示； | ***90%*** | <ol><li>验证改变系统配色方案侧边栏配色是否生效； |
| **布局** | **主要区域** | **头部导航** | <ol><li>显示控制侧边栏展开折叠的按钮且功能正常；<li>显示面包屑，用于显示当前页面的路径，快速返回之前的任意页面。最后一级的菜单不可以点击，页面夹类型的菜单点击回到页面夹下的第一个页面。多级嵌套页面类型的菜单点击应能正确返回，尤其是当前页面带参数并且上一级也带参数的情况，仅支持路径中的参数；<li>显示用户名称首个字符加背景色组成的简易头像，鼠标移上去可显示修改密码、系统设置和退出登录操作项；<li>刚初始化的用户推荐首次登录后自行修改密码，点击修改密码弹出修改密码弹窗，输入项有旧密码、新密码、确认密码，都为必填项，旧密码最长输入 20 个字符，新密码校验规则同系统管理下用户管理设置用户密码时的校验规则相同，确认密码必须与新密码一致。点击取消按钮关闭弹窗，点击确定按钮发起修改密码请求，请求过程中显示加载中状态且禁用取消按钮，请求成功应保证用户令牌失效并跳转到登录页面重新登录。重新打开修改密码弹窗应清空上一次填写内容并且清除表单验证信息；<li>点击退出登录即可退出系统并使用户令牌失效，退出成功强制刷新页面；<li>点击系统设置可从右往左打开系统设置侧边栏，可通过修改 src/setting.js 中的 showSettings 控制是否显示系统设置按钮，也就是说可选择是否对用户开放系统设置功能；<li>可正确显示标签视图； | ***100%*** |  |
| **布局** | **主要区域** | **头部导航标签视图** | <ol><li>标签视图是由所有历史打开过的页面所组成的可横向滚动的一系列页面标签，当所有标签加起来的宽度超过最大宽度，鼠标滚轮上下滚动或者按住 shift 鼠标滚轮上下滚动也就是转化成左右滚动，可以操控滚动条左右滚动以查看超出范围的标签；<li>路由参数中 affix 为 true 则这个页面标签会被固定在标签视图禁止移除，参数中 noTagView 为 true 则表示不将此页面添加到标签视图中，其余只要访问页面的完整路径不一样就会新增一个页面标签，包括查询字符串不一样的情况；<li>如果路由参数设置了路由名称并且是否缓存设置为是则应视为需要进行缓存的页面，并将其添加到缓存数组，也就是说会记住当前页面的状态，发生页面切换时会保持相应的状态；<li>每个页面标签都可拖动改变标签位置；<li>除了固定标签，其他标签通过点击标签右侧的关闭按钮关闭页面标签，如果关闭的是当前打开的页面且还有标签页，关闭后跳转到最后一个标签页，如果标签全被关闭则跳转到首页；<li>鼠标右键点击标签可以唤出菜单，可进行刷新、关闭、关闭其他、关闭所有操作。点击刷新，如果当前页有缓存则从缓存数组中删除该页然后重新进入该页。点击关闭与直接点击标签右侧关闭按钮的效果相同。点击关闭其他，只保留当前页关闭其他非固定页。点击关闭所有，关闭所有非固定标签。点击空白处或者滚动标签视图则关闭唤出的菜单；<li>通过其他方式切换页面应保证对应标签被激活并且自动滚动滚动条到合适位置； | ***100%*** | |
| **布局** | **主要区域** | **系统设置** | <ol><li>可修改主题配置，修改完成后浏览器会记住所选项，刷新页面保证不会切换其他主题，也可通过修改 src/setting.js 中的 theme 设置默认主题，浏览器记住的优先；<li>可修改布局是否显示系统标识，修改完成后浏览器会记住所选项，刷新页面保证不会改变，也可通过修改 src/setting.js 中的 sidebarLogo 设置默认选项，浏览器记住的优先；<li>可修改布局是否固定头部导航，修改完成后浏览器会记住所选项，刷新页面保证不会改变，也可通过修改 src/setting.js 中的 fixedHeader 设置默认选项，浏览器记住的优先；<li>可修改布局是否开启标签视图，修改完成后浏览器会记住所选项，刷新页面保证不会改变，也可通过修改 src/setting.js 中的 tagsView 设置默认选项，浏览器记住的优先； | ***100%*** | |
| **布局** | **其他** | **其他** | <ol><li>布局可通过在菜单管理中改变是否显示布局选项或者修改 src/router/index.js 中路由的 layout 参数默认值来控制是否显示，比如登录页面就默认不显示布局；<li>系统中的按钮等组件应全部支持更改主配色，编写代码开发更多功能时应注意这一点；<li>当访问未注册的路由时应跳转到 404 页面，在 404 页面判断出要访问的页面在系统中存在但当前用户不具备访问权限时跳转到 401 页面； | ***100%*** | |
| **系统管理** | **用户管理** | **新增用户** | <ol><li>点击新增按钮可显示新增用户弹窗；<li>填写用户名，必填，长度最长为 64 个字符，不可与已有用户名重复；<li>填写密码，必填，长度在 6 到 20 位，使用 md5 加密传输，服务端校验密码不为 32 位字符则报错提示；<li>填写昵称，非必填，长度最长为 64 个字符；<li>填写手机号码，非必填，校验正确的手机号格式；<li>填写邮箱，非必填，校验正确的邮箱格式；<li>选择用户性别，非必填，选项列表来源于字典；<li>设置用户状态，正常或者停用，选项列表来源于字典，默认为正常，停用之后禁止登录系统；<li>选择角色，必填，可多选，选项列表为所有已创建角色，角色为禁用状态时不可选；<li>填写备注，非必填，长度最长为 255 个字符；<li>点击取消按钮关闭弹窗；<li>点击确定按钮发起新增用户请求，请求中确定按钮显示加载中效果且禁用取消按钮，新增成功自动关闭弹窗刷新用户列表；<li>重新打开新增用户弹窗应清空上一次填写内容并且清除表单验证信息 | ***80%*** | <ol><li>验证停用效果；<li>验证角色设置是否生效；<li>验证登录是否生效及密码加密方式的更换； |
| **系统管理** | **用户管理** | **用户列表** | <ol><li>用户列表可正常显示ID、用户名、昵称、手机号、状态、创建时间；<li>用户列表分页功能正常，按创建时间倒序排列；<li>点击搜索按钮可根据可根据用户名、手机号码、用户状态查询用户列表；<li>点击重置可重置所有条件为空查询用户列表；<li>点击搜索和重置列表要回到第一页；| ***100%*** |  |
| **系统管理** | **用户管理** | **编辑用户** | <ol><li>点击用户列表每一行操作列的编辑按钮可显示编辑用户弹窗，弹窗可正确回显用户信息；<li>编辑时不可修改用户密码，因此不显示密码输入框，其他输入项及校验规则与新增时相同，点击取消按钮关闭弹窗，点击确定按钮发起编辑用户请求，请求中确定按钮显示加载中效果且禁用取消按钮，编辑成功关闭弹窗刷新用户列表；<li>禁止修改超级管理员用户 admin 的用户名、昵称、状态和角色；<li>用户列表状态列显示状态开关，可快速编辑用户状态（用户 admin 同样禁用这个快速操作），点击弹出二次确认弹窗，点击取消关闭弹窗，点击确定发起修改状态请求，请求中确定按钮显示加载中效果且禁用取消按钮，修改成功关闭弹窗刷新用户列表； | ***80%*** | <ol><li>验证停用效果；<li>验证角色设置是否生效；<li>验证登录是否生效； |
| **系统管理** | **用户管理** | **删除用户** | <ol><li>点击用户列表每一行操作列的删除按钮可显示删除用户二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，请求成功关闭弹窗刷新用户列表；<li>超级管理员用户 admin 禁止被删除； | ***95%*** | <ol><li>验证删除后不可登录；<li>验证用户角色关联表是否正常删除； |
| **系统管理** | **用户管理** | **重置密码** | <ol><li>点击用户列表每一行操作列的重置密码按钮可显示重置密码弹窗；<li>填写新密码，必填，校验规则同新增用户填写密码时相同；<li>点击取消关闭弹窗；<li>点击确认发起重置密码请求，请求中确定按钮显示加载中效果且禁用取消按钮，重置成功关闭弹窗刷新用户列表；<li>重新打开重置密码弹窗应清空上一次填写内容并且清除表单验证信息；<li>超级管理员用户 admin 禁止重置密码； | ***90%*** | <ol><li>验证重置后的密码是否生效； |
| **系统管理** | **角色管理** | **新增角色** | <ol><li>点击新增按钮可显示新增角色弹窗；<li>填写角色名称，必填，长度最长为 128 个字符，不可与已有角色名称重复；<li>填写角色标识，必填，长度最长为 128 个字符，不可与已有角色标识重复；<li>填写角色排序，必填，必须是数字，最小 0，最大 4294967295，默认填写 0；<li>设置用户状态，正常或者停用，默认为正常，停用之后不可与用户发生绑定，已绑定的应失效；<li>选择菜单权限，非必选，树形选择框，可多选，默认全不选、折叠、父子关联选择，有全选、展开、取消父子关联选择的选项可进行调整，树形数据要确保与菜单管理配置的一致；<li>填写备注，非必填，长度最长为 255 个字符；<li>点击取消按钮关闭弹窗；<li>点击确定发起新增角色请求，请求中确定按钮显示加载中效果且禁用取消按钮，新增成功关闭弹窗刷新角色列表；<li>重新打开新增角色弹窗应清空上一次填写内容并且清除表单验证信息； | ***80%*** | <ol><li>验证角色停用效果；<li>验证菜单树返回是否正确；<li>验证角色拥有的菜单是否生效； |
| **系统管理** | **角色管理** | **角色列表** | <ol><li>角色列表可正常显示ID、角色名称、角色标识、排序、状态、创建时间；<li>用户列表分页功能正常，按设置的角色顺序排序，角色顺序相同时按创建时间倒序排列；<li>点击搜索按钮可根据角色名称、角色标识、角色状态查询角色列表；<li>点击重置可重置所有条件为空查询角色列表；<li>点击搜索和重置列表要回到第一页；| ***100%*** |  |
| **系统管理** | **角色管理** | **编辑角色** | <ol><li>点击角色列表每一行操作列的编辑按钮可显示编辑角色弹窗，弹窗可正确回显角色信息；<li>编辑时输入项及校验规则与新增时相同。点击确认发起编辑角色请求，请求中确定按钮显示加载中效果且禁用取消按钮，编辑成功关闭弹窗刷新角色列表。默认超级管理员角色 admin 禁止修改其角色名称、角色标识和状态，不提供选择菜单权限功能，因为其默认拥有所有菜单权限；<li>角色列表状态列显示状态开关，可快速编辑角色状态，点击弹出二次确认弹窗。点击取消关闭弹窗。点击确定发起修改状态请求，请求中确定按钮显示加载中效果且禁用取消按钮，修改成功关闭弹窗刷新角色列表； | ***80%*** | <ol><li>验证角色停用效果；<li>验证菜单树返回是否正确；<li>验证角色拥有的菜单是否生效； |
| **系统管理** | **角色管理** | **删除角色** | <ol><li>点击角色列表每一行操作列的删除按钮可显示删除角色二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，请求成功关闭弹窗刷新角色列表；<li>超级管理员角色 admin 禁止被删除； | ***90%*** | <ol><li>验证删除角色后对应用户拥有的权限失效；<li>验证用户角色关联表及角色菜单关联表是否正常删除； |
| **系统管理** | **菜单管理** | **新增菜单** | <ol><li>点击新增按钮可显示新增菜单弹窗；<li>选择父级菜单，必选，默认为根目录，树形列表，只显示菜单类型不为按钮的菜单，默认全部展开显示，父级菜单不能选外部链接类型的页面；<li>父级菜单选择页面夹类型，菜单类型不受限制；<li>父级菜单选择页面，菜单类型不可选页面夹，自动将菜单类型设置为页面，显示高亮菜单输入项并自动设置为没有被隐藏的父菜单页面路径，是否显示菜单默认为隐藏且不可更改，是否为外链默认为否且不可更改；<li>填写菜单名称，必填，长度最长为 128 个字符；<li>填写排序，必须是数字，最小 0，最大 4294967295，默认填写 0；<li>选择菜单类型，必选，可选页面夹、页面、按钮，父菜单为页面时，仅可选页面、按钮；<li>填写权限标识，非必填，长度最长为 255 个字符，填入值不可与已有菜单的权限标识重复；<li>菜单类型选择页面夹或页面时，填写菜单图标，非必填，提供自定义 svg 图标和 element 自带图标可选择，在 /src/icons/svg 目录下添加 svg 图标可自动发现并添加到自定义 svg 图标列表中，在 /src/icons 目录下执行 svgo ./svg/xx.svg 可对单个 svg 图标进行精简缩减体积并删除 fill 属性，svgo ./svg/*.svg 可处理所有；<li>菜单类型选择页面夹或页面时，选择是否显示菜单，默认显示菜单，选择隐藏菜单则侧边栏菜单不会出现该菜单，父级菜单与菜单类型皆为页面时自动选择隐藏且禁用选项；<li>菜单类型选择页面时，选择是否为外部链接，默认否，父级菜单与菜单类型皆为页面时自动选择否且禁用选项，选择是时，仅需填写外部链接地址即可，后面描述的输入项可忽略，外链地址必填，长度最长为 255 个字符，必须匹配 /^(https?:\|mailto:\|tel:)/ 这个正则否则显示不支持的外部链接地址；<li>菜单类型选择页面且不为外部链接时，选择是否使用缓存，默认为不使用，如果选择使用，则路由名称为必填并提醒用户使用缓存时代码里的组件名称和路由名称要保持一致，否则缓存不生效；<li>菜单类型选择页面且不为外部链接时，选择是否显示布局，必填，默认为显示，选择不显示，则侧边栏菜单、顶部导航等布局元素都将不显示，仅显示页面内容；<li>菜单类型选择页面且不为外部链接时，填写路由名称，非必填，校验唯一性，长度最长为 128 个字符，选择使用缓存则路由名称必填，并提醒用户使用缓存时代码里的组件名称和路由名称要保持一致，否则缓存不生效； <li>菜单类型选择页面且不为外部链接时，填写组件路径，必填，长度最长为 255 个字符，必须以斜杠开头；<li>菜单类型选择页面且不为外部链接时，填写路由地址，必填，校验唯一性，长度最长为 255 个字符，必须以斜杠开头；<li>菜单类型选择页面且不为外部链接或者按钮时，选择页面要绑定的接口，非必选，可选择多个接口，使用穿梭框，可全选授权和全选取消授权，可批量授权和批量取消授权，可搜索接口；<li>点击取消按钮关闭弹窗；<li>点击确定按钮发起新增菜单请求，请求中确定按钮显示加载中效果且禁用取消按钮，新增成功关闭弹窗刷新菜单列表；<li>重新打开新增菜单弹窗应清空上一次填写内容并且清除表单验证信息； | **80%** | <ol><li>验证选择父级菜单时的树形列表返回是否正确；<li>验证给用户配置的菜单是否生效；<li>验证接口列表是否返回正确； |
| **系统管理** | **菜单管理** | **菜单列表** | <ol><li>搜索条件为空时菜单列表为树形结构，存在搜索条件时为展开的列表，树形结构应正确显示嵌套结构，能正常显示ID、菜单名称、图标、排序、权限标识、菜单类型、显示状态、创建时间；<li>鼠标移到权限标识上可浮窗显示所绑定的接口名称和接口信息，接口信息包括请求类型和接口路径；<li>列表不分页，按设置的顺序排序，顺序相同时按创建时间倒序排列；<li>点击搜索按钮可根据菜单名称、显示状态查询列表；<li>点击重置可重置所有条件为空查询列表；<li>点击列表中每一行菜单类型为页面夹或页面的操作列的新增按钮可显示新增菜单弹窗并将父级菜单设置为该页面夹或页面； | ***90%*** | <ol><li>验证菜单绑定接口列表是否正确； |
| **系统管理** | **菜单管理** | **编辑菜单** | <ol><li>点击列表每一行操作列的编辑按钮可打开编辑菜单弹窗，弹窗可正确回显菜单内容；<li>编辑的内容与格式校验等与新增相同；<li>禁止修改菜单类型；<li>编辑页面时不支持修改是否为外链选项；<li>父菜单不能是外部链接类型的页面；<li>编辑页面夹和页面时，父级菜单不能选自己；<li>编辑页面夹和页面时，父级菜单不能选自己的子菜单；<li>编辑页面夹时，父级菜单不能选页面；<li>默认禁止修改菜单管理这个菜单的权限标识输入项；<li>点击取消关闭弹窗；<li>点击确定发起修改请求，请求中确定按钮显示加载中效果且禁用取消按钮，修改成功关闭弹窗刷新菜单列表； | ***90%*** | <ol><li>验证给用户配置的菜单是否生效； |
| **系统管理** | **菜单管理** | **删除菜单** | <ol><li>点击列表每一行操作列的删除按钮可打开删除菜单二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，请求成功关闭弹窗刷新菜单列表；<li>默认菜单管理菜单禁止被删除，菜单包含菜单管理菜单也禁止被删除； | ***90%*** | <ol><li>验证给用户配置的菜单是否生效； |
| **系统管理** | **接口管理** | **新增接口** | <ol><li>点击新增按钮可显示新增接口弹窗；<li>填写接口名称，必填，长度最长为 128 个字符，不可与已有接口名称重复；<li>选择接口请求类型，必选，选项可通过字典管理动态增加或减少；<li>填写接口路径，必填，长度最长为 128 个字符，可重复；<li>点击取消按钮关闭弹窗；<li>点击确认发起新增接口请求，请求过程中显示加载中状态，请求成功关闭弹窗刷新接口列表；<li>重新打开新增接口弹窗应清空上一次填写内容并且清除表单验证信息； | ***90%*** | <ol><li>验证接口请求类型通过字典管理增减是否生效； |
| **系统管理** | **接口管理** | **接口列表** | <ol><li>接口列表可正常显示接口ID、接口名称、接口信息、创建时间，接口信息包括接口请求类型及接口路径；<li>列表分页功能正常，按创建时间倒序排列；<li>点击搜索按钮可根据接口名称、接口路径、请求类型查询列表；<li>点击重置可重置所有条件为空查询列表；<li>点击搜索和重置列表要回到第一页； | ***100%*** |  |
| **系统管理** | **接口管理** | **编辑接口** | <ol><li>点击列表每一行操作列的编辑按钮可打开编辑菜单弹窗，弹窗可正确回显接口内容；<li>编辑的内容与格式校验等与新增相同；<li>点击取消按钮关闭弹窗；<li>点击确认发起编辑接口请求，请求过程中显示加载中状态，请求成功关闭弹窗刷新接口列表；| ***100%*** |  |
| **系统管理** | **接口管理** | **删除接口** | <ol><li>点击列表每一行操作列的删除按钮可打开删除接口二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，请求成功关闭弹窗，刷新接口列表； | ***100%*** |  |
| **系统管理** | **字典管理** | **新增字典** | <ol><li>点击新增按钮可显示新增字典弹窗；<li>填写字典名称，必填，长度最长为 128 个字符，不可与已有字典名称重复；<li>填写字典类型，必填，长度最长为 128 个字符，不可与已有字典类型重复；<li>选择字典状态，非必选，默认为正常，选择停用则请求字典数据为空；<li>填写备注，非必填，长度最长为 255 个字符；<li>重新打开新增字典弹窗应清空上一次填写内容并且清除表单验证信息； | ***90%*** | <ol><li>验证停用是否生效； |
| **系统管理** | **字典管理** | **字典列表** | <ol><li>字典列表可正常显示ID、字典名称、字典类型、状态、备注、创建时间；<li>列表分页功能正常，按创建时间倒序排列；<li>点击搜索按钮可根据字典名称、字典类型、字典状态查询列表；<li>点击重置可重置所有条件为空查询列表；<li>点击搜索和重置列表要回到第一页； | ***100%*** |  |
| **系统管理** | **字典管理** | **编辑字典** | <ol><li>点击列表每一行的编辑按钮可打开编辑弹窗，弹窗可正确回显字典内容；<li>编辑的内容与格式校验等与新增相同；<li>点击取消按钮关闭弹窗；<li>点击确认发起编辑接口请求，请求过程中显示加载中状态，请求成功关闭弹窗刷新字典列表； | ***90%*** | <ol><li>验证停用是否生效； |
| **系统管理** | **字典管理** | **删除字典** | <ol><li>点击列表每一行操作列的删除按钮可打开删除字典二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，请求成功关闭弹窗，刷新字典列表；<li>删除后请求字典数据应返回空； | ***90%*** | <ol><li>验证删除后字典数据返回空是否生效； |
| **系统管理** | **字典数据管理** | **新增字典数据** | <ol><li>字典数据管理需要点击字典管理列表每一行字典类型列的按钮进入；<li>进入后点击新增按钮可显示新增字典数据弹窗；<li>填写标签，必填，长度最长为 128 个字符，不可与已有数据标签重复；<li>填写键值，必填，长度最长为 255 个字符，不可与已有数据键值重复；<li>填写显示排序，必须是数字，最小 0，最大 4294967295，默认填写 0；<li>选择字典数据状态，默认为正常，选择停用则请求字典数据时过滤此字典数据；<li>填写备注，非必填，长度最长为 255 个字符；<li>重新打开新增字典数据弹窗应清空上一次填写内容并且清除表单验证信息； | ***90%*** | <ol><li>验证停用是否生效； |
| **系统管理** | **字典数据管理** | **字典数据列表** | <ol><li>字典数据列表可正常显示ID、标签、键值、排序、状态、备注、创建时间；<li>列表分页功能正常，按排序值排序，排序值相同按创建时间倒序排列；<li>点击搜索按钮可根据标签、状态查询列表；<li>点击重置可重置所有条件为空查询列表；<li>点击搜索和重置列表要回到第一页； | ***100%*** |  |
| **系统管理** | **字典数据管理** | **编辑字典数据** | <ol><li>点击列表每一行操作列的编辑按钮可打开编辑弹窗，弹窗可正确回显字典数据内容；<li>编辑的内容与格式校验等与新增相同；<li>点击取消按钮关闭弹窗；<li>点击确认发起编辑接口请求，请求过程中显示加载中状态，请求成功关闭弹窗刷新字典数据列表； | ***100%*** |  |
| **系统管理** | **字典数据管理** | **删除字典数据** | <ol><li>点击列表每一行的删除按钮可打开删除字典数据二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，请求成功关闭弹窗，刷新字典数据列表； | ***90%*** | <ol><li>验证字典与字典数据关联表是否正常删除； |
| **系统管理** | **系统日志登录日志** | **日志列表** | <ol><li>日志列表可正常显示用户名、IP 地址、登录时间；<li>鼠标悬浮在 IP 地址上可显示悬浮框，框里面显示 IP 地址、浏览器和系统；<li>列表分页功能正常，按登录时间倒序排列；<li>点击搜索按钮可根据用户名、IP地址查询列表；<li>点击重置可重置所有条件为空查询列表；<li>点击搜索和重置列表要回到第一页； | ***100%*** |  |
| **系统管理** | **系统日志登录日志** | **删除日志** | <ol><li>点击列表每一行操作列的删除按钮可打开删除日志二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，请求成功关闭弹窗，刷新日志列表；<li>选中多条列表数据点击删除按钮可批量删除日志； | ***100%*** | |
