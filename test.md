| 页面     | 模块     | 功能项     | 描述                             | 进度   | 备注           |
|----------|---------|-----------|----------------------------------|-------|---------------|
| **登录页** | **登录表单** | **输入用户名密码** | <ol><li>页面加载成功后用户名输入框为聚焦状态；<li>用户名不能为空且最长输入 64 个字符；<li>用户名输入框右侧显示清除按钮，点击可以清除用户名输入框中的内容；<li>密码不能为空且最长输入 20 个字符；<li>在密码输入框中输入大写字母时提示已开启大写；<li>密码输入框右侧显示清除和显示密码按钮，点击可以完成对应功能；<li>用户名密码输入完成后点击登录按钮触发拼图验证； | ***100%*** |  |
| **登录页** | **登录表单** | **拼图验证码** | <ol><li>用户名密码输入完成后点击登录按钮弹出拼图验证码；<li>拼图验证码包括随机请求的一张图像和从图像中抠出的一块拼图；<li>抠图时选择的位置在图像的右半部分，抠出后原图像位置显示阴影；<li>被抠出的拼图放在图像最左边，保证一定的移动空间；<li>请求图像和抠出拼图的过程中显示加载中状态；<li>请求失败时显示加载失败；<li>加载成功后在拼图验证码下方显示滑轨和滑块；<li>点击左上角换一张按钮可更换拼图验证码；<li>点击右上角关闭按钮可关闭拼图验证码；<li>鼠标拖动滑块可移动拼图，松开鼠标结束拖动；<li>结束拖动后将滑块移动的距离与实际拼图位置做对比确定是否验证成功；<li>验证失败时提示拼图验证失败并重新请求拼图验证码；<li>验证成功后验证用户名密码；<li>用户名错误或者密码错误则显示用户名与密码不匹配并关闭拼图验证码弹窗，旧已验证正确的验证码要保证失效；<li>验证拼图或者用户名密码的过程中不可操作滑块、更换验证码，关闭拼图验证弹窗再打开要保持验证中的验证码状态；<li>验证码及用户名密码验证成功后滑轨显示拼接成功即登录成功； | ***90%*** | 14后半部分验证码失效未完成，其他已完成 |
| **登录页** | **其他** | **其他** | <ol><li>未登录成功时，访问不需登录即可访问的页面可直接进入，否则跳转到登录页面，并设置登录页面的 query 为将要访问页面的 query，在 query 中添加 redirect 参数，设置其值为将要访问的页面地址；<li>登录成功后如果 query 中存在 redirect 参数则跳转到 redirect 指定的页面并将其他同在 query 中的参数绑定给这个页面，这意味着 query 中的redirect参数名为保留名，应避免重名，一旦发生重名，页面地址会覆盖已设置的参数值，访问对应页面时会丢失一个参数；<li>登录成功后可以保持登录状态，在浏览器地址栏输入登录页面的地址后会重新回到首页；<li>登录状态最长可以保持 7 天，在 7 天内没有任何操作则会强制重新登录，在临近到期的最后 30 分钟内发起操作会重新延长登录状态到 7 天；<li>首次登录成功后会请求用户信息并缓存到 vuex 中，用户信息包括用户名、用户角色、用户页面及按钮权限标识，拥有admin角色的用户页面及按钮权限标识为[“\*:\*:\*”]；<li>登录成功后只要缓存的用户信息中角色存在且不为空则切换页面不会重新请求用户信息，但是每次刷新页面会重新请求用户信息；<li>用户信息请求成功则继续请求用户菜单路由，拥有 admin 角色的用户拥有所有菜单路由，其他角色用户根据设置的权限获取菜单路由，并按设置的排序正确返回，同时获取系统全部的菜单路由路径，目的是为了用户在访问未拥有的页面时提示 401 无权限；<li>将菜单路由转换成渲染菜单及注册路由需要的格式并将数据缓存到 vuex，将系统全部的路由路径转换为路径正则缓存到 vuex；<li>用户信息与用户菜单路由请求过程中，页面顶部显示进度条；<li>用户信息或者用户菜单路由请求失败时，控制台打印错误信息，清除token，清除用户角色，回到登录页； | ***99%*** | 7排序不正确未完成，其他完成 |
| **系统管理** | **用户管理** | **新增用户** | <ol><li>点击新增按钮可显示新增用户弹窗；<li>填写用户名，必填，长度最长为 64 个字符，不可与已有用户名重复；<li>填写密码，必填，长度在 6 到 20 位，使用 md5 加密传输，服务端校验密码不为 32 位字符则报错提示；<li>填写昵称，必填，长度最长为 64 个字符；<li>填写手机号码，必填，校验正确的手机号格式；<li>填写邮箱，非必填，校验正确的邮箱格式；<li>选择用户性别，非必填；<li>设置用户状态，非必填，正常或者停用，默认为正常，停用之后禁止登录系统；<li>选择角色，必填，可多选；<li>填写备注，非必填，长度最长为 255 个字符；<li>点击取消按钮关闭弹窗；<li>点击确定按钮发起新增用户请求，请求中确定按钮显示加载中效果且禁用取消按钮，新增成功关闭弹窗刷新用户列表； | ***90%*** | <ol><li>验证停用效果；<li>验证角色设置是否生效；<li>验证登录是否生效 |
| **系统管理** | **用户管理** | **用户列表** | <ol><li>用户列表可正常显示ID、用户名、昵称、手机号、状态、创建时间；<li>用户列表分页功能正常，按创建时间倒序排列；<li>点击搜索按钮可根据可根据用户名、手机号码、用户状态查询用户列表；<li>点击重置可重置所有条件为空查询用户列表；<li>点击搜索和重置列表要回到第一页；| ***100%*** |  |
| **系统管理** | **用户管理** | **编辑用户** | <ol><li>点击用户列表每一行的编辑按钮可显示编辑用户弹窗，弹窗可正确回显用户信息；<li>编辑时不可显示和修改用户密码，因此不显示密码输入框，其他输入项及校验规则与新增时相同；<li>点击取消按钮关闭弹窗；<li>点击确定按钮发起编辑用户请求，请求中确定按钮显示加载中效果且禁用取消按钮，编辑成功关闭弹窗刷新用户列表；<li>禁止修改超级管理员用户 admin 的用户名、昵称、状态和角色；<li>用户列表状态列显示状态开关，可快速编辑用户状态，点击弹出二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确定发起修改状态请求，请求中确定按钮显示加载中效果且禁用取消按钮，修改成功关闭弹窗刷新用户列表； | ***90%*** | <ol><li>验证停用效果；<li>验证角色设置是否生效；<li>验证登录是否生效 |
| **系统管理** | **用户管理** | **删除用户** | <ol><li>点击用户列表每一行的删除按钮可显示删除用户二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，请求成功关闭弹窗刷新用户列表；<li>超级管理员用户 admin 禁止被删除； | ***95%*** | <ol><li>验证删除后不可登录 |
| **系统管理** | **用户管理** | **重置密码** | <ol><li>点击用户列表每一行的重置密码按钮可显示重置密码弹窗；<li>填写新密码，必填，校验规则同新增用户填写密码时相同；<li>点击取消关闭弹窗；<li>点击确认发起重置密码请求，请求中确定按钮显示加载中效果且禁用取消按钮，重置成功关闭弹窗刷新用户列表；<li>超级管理员用户 admin 禁止重置密码； | ***95%*** | 验证重置后的密码是否生效 |
| **系统管理** | **角色管理** | **新增角色** | <ol><li>点击新增按钮可显示新增角色弹窗；<li>填写角色名称，必填，长度最长为 128 个字符，不可与已有角色名称重复；<li>填写角色标识，必填，长度最长为 128 个字符，不可与已有角色标识重复；<li>填写角色排序，必填，必须是数字，最小 0，最大 4294967295，默认填写 0；<li>设置用户状态，正常或者停用，停用之后不可与用户发生绑定，已绑定的应失效；<li>选择菜单权限，非必选，树形选择框，可多选，默认全不选、折叠显示，有全选、展开的选项可进行调整，树形数据要确保与菜单管理配置的一致；<li>填写备注，非必填，长度最长为 255 个字符；<li>点击确认发起新增角色请求，请求中确定按钮显示加载中效果且禁用取消按钮，新增成功关闭弹窗刷新角色列表； | ***80%*** | <ol><li>角色停用效果未验证；<li>菜单树返回是否正确待确认；<li>角色拥有的菜单是否生效待验证 |
| **系统管理** | **角色管理** | **角色列表** | <ol><li>角色列表可正常显示ID、角色名称、角色标识、排序、状态、创建时间；<li>用户列表分页功能正常，按设置的角色顺序排序，角色顺序相同时按创建时间倒序排列；<li>点击搜索按钮可根据角色名称、角色标识、角色状态查询角色列表；<li>点击重置可重置所有条件为空查询角色列表；<li>点击搜索和重置列表要回到第一页；| ***100%*** |  |
| **系统管理** | **角色管理** | **编辑角色** | <ol><li>点击角色列表每一行的编辑按钮可显示编辑角色弹窗，弹窗可正确回显角色信息；<li>填写角色名称，必填，长度最长为 128 个字符，不可与其他角色名称重复；<li>填写角色标识，必填，长度最长为 128 个字符，不可与其他角色标识重复；<li>填写角色顺序，必须是数字，最小 0，最大 100000；<li>设置用户状态，必填，正常或者停用，停用之后不可与用户发生绑定，已绑定的做失效处理；<li>选择菜单权限，非必选，树形选择框，默认全部展开，可多选，父子不做严格关联选择；<li>填写备注，非必填，长度最长为 255 个字符；<li>点击确认发起编辑角色请求，请求过程中显示加载中状态，请求成功关闭弹窗刷新角色列表；<li>默认超级管理员角色 admin 禁止修改其角色标识和状态且不支持选择菜单权限，其默认拥有所有菜单权限；<li>点击角色列表每一行的状态开关可快速编辑角色状态，需要二次确认； | ***90%*** | 5做失效处理未测试 |
| **系统管理** | **角色管理** | **删除角色** | <ol><li>点击角色列表每一行的删除按钮可显示删除角色二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，关闭弹窗，删除成功刷新角色列表；<li>默认超级管理员角色 admin 禁止被删除； | ***100%*** |  |
| **系统管理** | **菜单管理** | **新增菜单** | <ol><li>点击列表上方的新增按钮可显示新增菜单弹窗并将父级菜单设置为根目录；<li>选择父级菜单，必选，树形列表，点击可选择，只显示菜单类型不为按钮的菜单；<li>父级菜单选择页面夹，菜单类型不受限制；<li>父级菜单选择页面，菜单类型不可选页面夹，自动将菜单类型设置为页面，显示高亮菜单输入项并自动设置为没有被隐藏的父菜单页面路径，是否显示菜单默认为否且不可更改，是否为外链默认为否且不可更改；<li>填写菜单名称，必填，长度最长为 128 个字符；<li>填写排序，必填，必须是数字，最小 0，最大 100000，默认填写 0；<li>选择菜单类型，必选，可选页面夹、页面、按钮，父菜单为页面时，仅可选页面、按钮；<li>填写权限标识，非必填，长度最长为 255 个字符；<li>菜单类型选择页面夹或页面时，填写菜单图标，非必填，提供自定义 svg 图标和 element 自带图标可选择，在 /src/icons/svg 目录下添加 svg 图标可自动发现添加到自定义 svg 图标列表中，在 /src/icons 目录下执行 svgo ./svg/xx.svg 可对单个 svg 图标进行精简缩减体积并删除 fill 属性，svgo ./svg/*.svg 可处理所有；<li>菜单类型选择页面夹或页面时，选择是否显示菜单，默认显示菜单，选择隐藏菜单则侧边栏菜单不会出现该菜单，父级菜单与菜单类型皆为页面时自动选择隐藏且禁用选项；<li>菜单类型选择页面时，选择是否为外部链接，默认否，父级菜单与菜单类型皆为页面时自动选择否且禁用选项，选择是时，仅需填写外部链接地址即可，后面描述的输入项可忽略，外链地址必填，长度最长为 255 个字符，必须匹配 /^(https?:\|mailto:\|tel:)/ 这个正则否则显示不支持的外部链接地址；<li>菜单类型选择页面时，选择是否使用缓存，必填，默认为不使用，如果选择使用，则路由名称为必填并提醒用户使用缓存时代码里的组件名称和路由名称要保持一致，否则缓存不生效；<li>菜单类型选择页面时，选择是否显示布局，必填，默认为显示，选择不显示，则侧边栏菜单、顶部导航等布局元素都将不显示，仅显示页面内容；<li>菜单类型选择页面时，填写路由名称，非必填，校验唯一性，长度最长为 128 个字符，选择使用缓存则路由名称必填，并提醒用户使用缓存时代码里的组件名称和路由名称要保持一致，否则缓存不生效； <li>填写组件路径，必填，长度最长为 255 个字符，必须以斜杠开头；<li>填写路由地址，必填，校验唯一性，长度最长为 255 个字符，必须以斜杠开头；<li>菜单类型选择页面或者按钮时，选择页面要绑定的接口，非必选，可选择多个接口，使用穿梭框，可全选授权和取消授权，可批量授权和批量取消授权，可搜索接口；<li>点击取消按钮关闭弹窗；<li>点击确定按钮发起新增菜单请求，请求中显示加载中效果，新增成功关闭弹窗刷新菜单列表；| ***100%*** |  | ***100%*** |  |
| **系统管理** | **菜单管理** | **菜单列表** | <ol><li>搜索条件为空时菜单列表为树形结构，存在搜索条件时为展开的列表，可正确显示嵌套结构，能正常显示ID、菜单名称、图标、排序、权限标识、菜单类型、显示状态、创建时间；<li>鼠标移到权限标识上可浮窗显示所绑定的接口名称和接口信息，接口信息包括请求类型和接口路径；<li>列表不分页，按设置的顺序排序，顺序相同时按创建时间倒序排列；<li>点击搜索按钮可根据菜单名称、显示状态查询列表；<li>点击重置可重置所有条件为空查询列表；<li>点击列表中菜单类型为页面夹或页面的每一行的新增按钮可显示新增菜单弹窗并将父级菜单设置为该页面夹或页面； | ***100%*** |  |
| **系统管理** | **菜单管理** | **编辑菜单** | <ol><li>点击列表每一行的编辑按钮可打开编辑菜单弹窗，弹窗可正确回显菜单内容；<li>编辑的内容与格式校验等与新增相同；<li>编辑页面夹和页面时，父级菜单不能选自己； | ***100%*** |  |
| **系统管理** | **菜单管理** | **删除菜单** | <ol><li>点击列表每一行的删除按钮可打开删除菜单二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，关闭弹窗，请求成功刷新菜单列表；<li>默认菜单管理菜单禁止被删除，菜单包含菜单管理菜单也禁止被删除； | ***100%*** |  |
| **系统管理** | **接口管理** | **新增接口** | <ol><li>点击新增按钮可显示新增接口弹窗；<li>填写接口名称，必填，长度最长为 128 个字符，不可与已有接口名称重复；<li>选择接口请求类型，必选，选项可通过字典管理动态增加；<li>填写接口路径，必填，长度最长为 128 个字符，可重复；<li>点击取消按钮关闭弹窗；<li>点击确认发起新增接口请求，请求过程中显示加载中状态，请求成功关闭弹窗刷新接口列表； | ***100%*** |  |
| **系统管理** | **接口管理** | **接口列表** | <ol><li>接口列表可正常显示接口名称、接口信息、创建时间，接口信息包括接口请求类型及接口路径；<li>列表分页功能正常，按创建时间倒序排列；<li>点击搜索按钮可根据接口名称、接口路径、请求类型查询列表；<li>点击重置可重置所有条件为空查询列表；<li>点击搜索和重置列表要回到第一页； | ***100%*** |  |
| **系统管理** | **接口管理** | **编辑接口** | <ol><li>点击列表每一行的编辑按钮可打开编辑菜单弹窗，弹窗可正确回显接口内容；<li>编辑的内容与格式校验等与新增相同；| ***100%*** |  |
| **系统管理** | **接口管理** | **删除接口** | <ol><li>点击列表每一行的删除按钮可打开删除接口二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，关闭弹窗，请求成功刷新接口列表； | ***100%*** |  |
| **系统管理** | **字典管理** | **新增字典** | <ol><li>点击新增按钮可显示新增字典弹窗；<li>填写字典名称，必填，长度最长为 128 个字符，不可与已有字典名称重复；<li>填写字典类型，必填，长度最长为 128 个字符，不可与已有字典类型重复；<li>选择字典状态，非必选，默认为正常，选择停用则请求字典数据为空；<li>填写备注，非必填，长度最长为 255 个字符； | ***100%*** |  |
| **系统管理** | **字典管理** | **字典列表** | <ol><li>字典列表可正常显示ID、字典名称、字典类型、状态、备注、创建时间；<li>列表分页功能正常，按创建时间倒序排列；<li>点击搜索按钮可根据字典名称、字典类型、字典状态查询列表；<li>点击重置可重置所有条件为空查询列表；<li>点击搜索和重置列表要回到第一页； | ***100%*** |  |
| **系统管理** | **字典管理** | **编辑字典** | <ol><li>点击列表每一行的编辑按钮可打开编辑弹窗，弹窗可正确回显字典内容；<li>编辑的内容与格式校验等与新增相同； | ***100%*** |  |
| **系统管理** | **字典管理** | **删除字典** | <ol><li>点击列表每一行的删除按钮可打开删除字典二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，关闭弹窗，请求成功刷新字典列表； | ***100%*** |  |
| **系统管理** | **字典数据管理** | **新增字典数据** | <ol><li>点击新增按钮可显示新增字典数据弹窗；<li>填写标签，必填，长度最长为 128 个字符，不可与已有数据标签重复；<li>填写键值，必填，长度最长为 255 个字符，不可与已有数据键值重复；<li>填写显示排序，必填，必须是数字，最小 0，最大 100000，默认填写 0；<li>选择字典数据状态，非必选，默认为正常，选择停用则过滤此字典数据；<li>填写备注，非必填，长度最长为 255 个字符； | ***100%*** |  |
| **系统管理** | **字典数据管理** | **字典数据列表** | <ol><li>字典列表可正常显示ID、标签、键值、排序、状态、备注、创建时间；<li>列表分页功能正常，按排序值排序，排序值相同按创建时间倒序排列；<li>点击搜索按钮可根据标签、状态查询列表；<li>点击重置可重置所有条件为空查询列表；<li>点击搜索和重置列表要回到第一页； | ***100%*** |  |
| **系统管理** | **字典数据管理** | **编辑字典数据** | <ol><li>点击列表每一行的编辑按钮可打开编辑弹窗，弹窗可正确回显字典数据内容；<li>编辑的内容与格式校验等与新增相同； | ***100%*** |  |
| **系统管理** | **字典数据管理** | **删除字典数据** | <ol><li>点击列表每一行的删除按钮可打开删除字典数据二次确认弹窗；<li>点击取消关闭弹窗；<li>点击确认发起删除请求，关闭弹窗，请求成功刷新字典数据列表； | ***100%*** |  |
